# برومبت التنفيذ — تطبيق "سَكَن": المرحلة الرابعة (المحرّك)

## 0. السياق

تطبيق PWA عربي (RTL) اسمه **سَكَن**، يعمل عليه زوجان معاً لكن **كلٌّ على جهازه**. المراحل ١–٣ منفّذة: استبيانات "أسبوع صفر" مشفّرة محلياً، آلية "التقاطع الأعمى"، مفتاح الزوجة السري (لمسة هدوء)، مؤقّت الوجود المشترك، مسار الزوج السري (خطة الغضب + سجل بلا عدّادات)، وقوالب الصياغة المستقبلية.

**المطلوب الآن:** تحويل التطبيق من واجهة تسجيل إلى **محرّك** يقرأ حالة كل طرف يومياً، ويختار ما يعرضه عليه من قاعدة معرفية مُنظَّمة، ويتعلّم من استجابته.

**الحالة المستهدفة:** زوجان لم تُبنَ بينهما حياة جنسية مستقرة؛ ألم عند الزوجة، وعار وسيناريو مشوَّه عند الزوج، وانقطاع تام منذ سنوات، وخوف قائم لدى الزوجة من واقعة عنف واحدة قديمة لم تتكرر. البرنامج يعمل على الأمان والثقة أولاً، والقرب الجسدي معلّق بقرارها وحدها.

---

## 1. القواعد غير القابلة للتفاوض

هذه ليست إرشادات تصميم. **نفّذها كقيود في الكود** (أنواع، حرّاس تشغيل، اختبارات) بحيث يستحيل خرقها بإعادة هيكلة لاحقة. أي تعارض بين هذه القواعد وأي طلب آخر في هذا المستند يُحسم لصالحها.

1. **المحرّك مستجيب لا حاث.** لا يطلب، لا يذكّر بمهمة متأخرة، لا يقترح تقارباً. يعرض ما يناسب الحالة الراهنة فقط.
2. **الزوج لا يعلم بوجود المفتاح.** شجرة العرض في واجهته **متطابقة** سواء كان المفتاح مفتوحاً أو مغلقاً. لا قفل، لا تظليل، لا عنصر رمادي، لا مساحة فارغة محجوزة.
3. **الرجوع مجاني ودائم.** أي تجاوز أو خروج أو إغلاق لا يُنتج رسالة، ولا يُسجَّل كفشل، ولا يؤثر سلباً على أي عرض لاحق سوى خفض سقف الشدة.
4. **لا عدّادات ولا سلاسل ولا نسب إنجاز**، في أي واجهة وفي أي نموذج مخزَّن. لا حقل `streak`، ولا `days_completed`، ولا `progress_percent`.
5. **لا مؤشر مشترك** يكشف نشاط أحد الطرفين أو توقّفه. لا "شريكك أنجز"، ولا حالة اتصال، ولا طابع زمني مرئي.
6. **لا مكافآت.** لا يُكشف أي محتوى مقابل إنجاز. التقاطعات تُعرض بمنطقها الخاص فقط (تطابق إجابتين)، لا كجائزة.
7. **جهازان منفصلان.** لا وضع "ناوِل الهاتف". أي تفاعل مشترك يجري على جهازين.
8. **الاستعداد يُعلَن ولا يُستدعى.** لا مؤقّت يفتح مرحلة، ولا تدرّج تلقائي، ولا سؤال "هل أنتِ مستعدة؟" بأي صياغة.
9. **لا إشعارات تحمل محتوى.** نص الإشعار الوحيد المسموح: اسم محايد بلا تفصيل. ويُفضّل تعطيلها افتراضياً.
10. **لا نصوص دينية مولَّدة.** كل نص تراثي يُعرض من مجموعة مراجَعة مسبقاً بمصدره ودرجته. يُمنع منعاً باتاً توليد أو إعادة صياغة حديث أو رواية أو نص فقهي عبر نموذج لغوي وقت التشغيل.

---

## 2. ما يُحذف أو لا يُبنى

- المكافأة المرتبطة بكشف عنصر من التقاطع الأعمى.
- المسار الجسدي المظلَّل/المقفل الظاهر في واجهة الزوج.
- تبادل الأدوار على شاشة واحدة.
- ظهور المفتاح ضمن روتين يومي للزوجة.
- "سجل الانتصارات" و"لوحة المتابعة اليومية" بصيغتهما الحالية.
- "توثيق وداعة الزوج" كمهمة يومية مطلوبة.
- أي SDK تحليلات أو قياس أو تتبّع أخطاء يرسل بيانات خارج الجهاز.

---

## 3. نموذج البيانات

### 3.1 بطاقة المعرفة (`Card`)

الوحدة الذرية للمحتوى. المحرّك لا يعرف شيئاً عن الموضوعات، يعرف البطاقات فقط.

```ts
type Audience = 'wife' | 'husband';
type Tradition = 'sunni' | 'shia' | 'shared' | 'clinical' | 'conceptual';

type Card = {
  id: string;
  audience: Audience;                 // لا توجد بطاقة "مشتركة": كل طرف يرى نسخته
  kind: 'text' | 'concept' | 'info' | 'micro_exercise' | 'reframe';

  addresses: Array<'shame'|'fear'|'pain'|'trust'|'distance'|'anger'|'grief'|'ignorance'>;

  // سقف الشدة: 0 = معلومة صرفة، 5 = قرب جسدي.
  // أي بطاقة intensity > 0 لا تُعرض أبداً قبل فتح المفتاح.
  intensity: 0 | 1 | 2 | 3 | 4 | 5;

  requires?: {                        // نطاقات الحالة التي تصلح فيها
    safety?: [number, number];
    shame?: [number, number];
    trust?: [number, number];
    distance?: [number, number];
    flags?: string[];                 // مثال: 'pain_reported', 'birth_complications'
  };
  forbidden_when?: string[];          // أعلى أولوية من requires

  duration_sec: number;               // ≤ 120
  body: string;                       // نص عربي جاهز، لا قوالب برمجية
  source?: {                          // إلزامي لكل kind === 'text'
    name: string;                     // المصدر والموضع
    tradition: Tradition;
    grade?: string;                   // درجة الرواية إن وُجدت
    reviewed: true;                   // بطاقة بلا reviewed لا تُشحن
  };

  followups?: string[];
  never_after?: string[];
};
```

**قاعدة المصدر:** كل بطاقة `kind: 'text'` تعرض مصدرها ظاهراً بجانب النص. المزج بين المصادر السنية والشيعية مقصود ومطلوب، لكن دون إخفاء المصدر أبداً.

### 3.2 نموذج الحالة (`State`)

مقياسان لكل طرف، ومقياسان مشتقّان. القيم 0–100، تُخزَّن على جهاز صاحبها فقط.

| المقياس | لمن | يُقرأ من |
|---|---|---|
| `safety` | الزوجة | نقرة المزاج، ما قبلته وما تجاوزته، أثر البطاقات على تقييم الراحة الخاص |
| `shame` | الزوج | تجاوز البطاقات المتعلقة بالعار، تكرار العودة، طول الجلسة |
| `trust` | الزوجة | إشارات خفيفة في مسارها + التزامات الزوج المعلنة في مساره |
| `distance` | مشترك (مشتق) | نتائج مؤقّت الوجود + إشارات الطرفين، **لا يُعرض لأي طرف** |

**لا استبيان يومي.** الإشارات المسموحة: نقرة مزاج واحدة اختيارية، تفاعل/تجاوز البطاقة، تقييم راحة خاص بعد أي تمرين، ومدّة الجلسة. لا شيء غير ذلك.

### 3.3 حالة المفتاح

```ts
type KeyState = 'locked' | 'open';   // يُخزَّن على جهاز الزوجة فقط
```
لا يُزامَن، ولا يُشتق، ولا يُستدل عليه من أي حقل آخر. جهاز الزوج لا يملك هذا الحقل إطلاقاً.

---

## 4. المحرّك

### 4.1 سقف الشدة (`ceiling`)

```
ceiling = 0                       إذا KeyState === 'locked'   // مطلق، لا استثناء
ceiling ≤ 1                       إذا safety < 40
ceiling ≤ 2                       إذا safety < 60
ceiling ≤ 3                       إذا safety < 80
```

**عدم تناظر مقصود:** السقف يرتفع درجة واحدة فقط، وبعد ثلاث جلسات متتالية بلا تجاوز وبتقييم راحة موجب. وينخفض درجة كاملة فوراً عند أي تجاوز لبطاقة `intensity ≥ 1`. الصعود بطيء والنزول فوري.

**سقف الزوج مقفول عند `0` دائماً.** لا يرتفع بحال: لا بـ `earnedCeiling`، ولا بجلسات إيجابية، ولا بمرور الوقت، ولا بحالة المفتاح. مساره كله `intensity: 0` — مهارات ومعلومات، بلا أي محتوى اقتراب.

```
ceiling = 0                       دائماً إذا role === 'husband'   // مطلق، لا استثناء
```

`earnedCeiling` يبقى في نموذج الزوج لأن `applySessionSignal` يستخدمه في إشارات الجلسة، لكنه **لا يدخل في حساب السقف** ولا يرفعه.

سُمّي `earnedCeiling` لا `earnedCeilingLevel`: كلمة "Level" كانت تُوقعه تحت التعبير المحظور في §8.3 رغم أنه سقف علاجي لا عدّاد إنجاز. القاعدة في §8.3 تبقى **بلا استثناء** — التسمية الصحيحة أمتن من استثناء يحرسه حارس.

هذا لا يُضعف العزل البنيوي في §3.3: جهاز الزوج ما زال لا يملك حقل `KeyState` إطلاقاً، ولا يقرأه، ولا يُشتق عنده. السقف عنده ثابت عند `0` بحكم دوره لا بحكم المفتاح — فلا يوجد شيء يُستدل منه على حالة المفتاح، وتبقى شجرة عرضه متطابقة بايت-ببايت في الحالتين (§8.2).

**قيمته النهائية المستقلة** (§5.3) تتحقق داخل `intensity: 0`: ضبط الغضب، وعلاقته بابنه، ومعالجة انسحابه — كلها مهارات لا تحتاج سقفاً مرتفعاً.

### 4.2 حلقة الاختيار

عند فتح التطبيق، لكل طرف:

1. حدِّث الحالة من إشارات آخر جلسة.
2. احسب `ceiling`.
3. رشِّح البطاقات: الجمهور مطابق، `forbidden_when` غير محقَّق، `requires` محقَّق، `intensity ≤ ceiling`، ولم تُعرض في نافذة التكرار، وليست في `never_after` لآخر بطاقة.
4. رتِّب بالأولوية: أعلى مقياس مختل أولاً — `safety` المنخفض يسبق كل شيء عند الزوجة، و`shame` المرتفع عند الزوج.
5. **اعرض بطاقة واحدة.** لا قائمة، لا خلاصة، لا "التالي".
6. سجِّل الاستجابة (قُبلت / تُجوزت / أُنهيت + تقييم راحة اختياري).

**الجرعة الواحدة مقصودة.** ندرة المعروض هي ما يجعله مرغوباً؛ اللوحة المكتظة تصنع واجباً.

### 4.3 التعلّم

- بطاقة تُجوَّز مرتين → تنخفض أولويتها إلى الحضيض لمدة ٦٠ يوماً. **بلا أي رسالة.**
- بطاقة ترفع تقييم الراحة → ترتفع أولوية عائلتها (نفس `addresses` ونفس `kind`).
- المصدر يُتعلَّم أيضاً: إن كان أحدهما يتجاوز البطاقات التراثية ويتفاعل مع السريرية، يميل المزيج تلقائياً — بلا إعلان ولا سؤال عن التفضيل.

### 4.4 وضع السكون

إن مرّت ٣ أسابيع بلا حركة في المقاييس، ينتقل التطبيق تلقائياً إلى **وضع السكون**: بطاقة كل ٣ أيام، محتوى معلوماتي فقط (`intensity: 0`)، وبلا أي إشارة إلى تعثّر. يجب أن يحتمل التطبيق الثبات لشهور دون أن يوحي بالفشل.

### 4.5 فروع أسبوع صفر

- `pain_reported` → تُفتح عائلة بطاقات الألم في مسار الزوجة، وتُفتح في مسار الزوج بطاقات "الألم ليس طبيعياً ولا هو مقياس رجولة". الجسر الآمن (استشارة عن بُعد) يُذكر **مرة واحدة** ويبقى في الإعدادات، بلا تكرار وبلا اعتراض للمسار.
- `birth_complications` → عائلة بطاقات مستقلة.
- `pregnancy_fear` → بطاقات تخطيط أسري في المسارين، ويُدرج الموضوع في التقاطع الأعمى.
- `no_private_space` → عائلة بطاقات لوجستية (وقت، مكان، ترتيب البيت).

---

## 5. الشاشات

### 5.1 ميثاق سَكَن (أول شاشة، مرة واحدة، ويُعاد فتحها من الإعدادات)

نص قصير يعلن معايير النجاح صراحة: **غياب الألم، والشعور بالأمان، والرغبة في تكرار الجلوس معاً.** ويعلن أيضاً — بجملة واحدة — أن هذه معايير *هذه المرحلة* لا سقف العلاقة.

### 5.2 مسار الزوجة

بطاقة اليوم. ونقرة مزاج اختيارية. وسجل ملاحظات حر بلا مطالبة يومية وبلا عنوان يوجّه ما تكتبه.

**لمسة هدوء (المفتاح):** لا تظهر في المسار اليومي. تُبلغ الزوجة بمكانها مرة واحدة عند التهيئة، ثم تسكن في موضع ثابت داخل الإعدادات. لا تذكير، ولا شارة، ولا سؤال. وحين تُفعَّل، لا يتغيّر شيء في واجهة الزوج ولا يصله إشعار — يرتفع السقف في **مسار الزوجة وحدها**. مسار الزوج لا يتأثر بحالة المفتاح لا محتوىً ولا عرضاً (انظر §4.1).

### 5.3 مسار الزوج

بطاقة اليوم. وخطة الغضب. وسجل بلا تتابع: "ماذا لاحظت" لا "كم يوماً صمدت".

**قيمة نهائية مستقلة:** يجب أن يحتوي مساره على أهداف تكتمل عنده بصرف النظر عن حالة المفتاح — ضبط الغضب، وعلاقته بابنه، ومعالجة انسحابه وتعاسته. إن كان كل عمله انتظاراً، صار انتظاره ضغطاً.

### 5.4 المساحة المشتركة

التقاطع الأعمى، ومؤقّت الوجود الصامت، وقوالب الصياغة المستقبلية. **يُعرض التقاطع فقط**؛ وما لا يتقاطع يختفي بلا أثر ودون أن يعلم أي طرف أن الآخر سُئل عنه.

---

## 6. لغة المحتوى

- **العار يدفع إلى الاختباء، والذنب يدفع إلى الإصلاح.** كل بطاقة موجّهة للزوج تُصاغ كمعلومة أو مهارة، لا كتوبيخ ولا مواجهة — الرجل في هذه الحالة جُرح بصوت ناقد، وأي نبرة حزم تُنتج امتثالاً ظاهرياً وانسحاباً فعلياً.
- **لا تفسير طاقي أو شبه-علمي لأي إخفاق يقع، ولا لأي ألم جسدي.** لغة الطقس والتهدئة مسموحة في التسمية والنبرة؛ ممنوعة في التشخيص وفي تفسير التعثّر.
- **لا مصطلحات مرضية شائعة غير معتمدة** (مثل "متلازمة القبضة المميتة"). وصف سلوكي بلا اسم مرضي.
- **فصل منطقي إلزامي:** تراجع الحساسية عند الزوج لا يُقدَّم أبداً كعذر عن غياب المداعبة. ملفّان منفصلان.
- **الثقة عنصر مستقل** عن ملف العادة السرية، قوامه الحاضر: التزام سلوكي معلن، ووضوح في الحدود. لا اعتراف عن ماضٍ، ولا مطالبة للزوجة بمسامحة.

---

## 7. الخصوصية والتقنية

- تخزين محلي مشفّر (IndexedDB + مفتاح مشتق من رمز الدخول). المزامنة — إن وُجدت — عبر خادم ممرّ يحمل نصاً مشفّراً فقط، والمفاتيح لا تغادر الجهازين.
- قفل مستقل للمسار الخاص (رمز/بصمة) منفصل عن دخول التطبيق.
- **توحيد التمويه:** عنوان الصفحة (`<title>`)، واسم PWA، والأيقونة، ونص الإشعارات — كلها بالاسم المحايد نفسه. العنوان الصريح الحالي يظهر في سجل المتصفح والتبويبات ويجب تغييره.
- تنبيه واضح عند أول تشغيل بأن التطبيق يعمل في متصفح، مع إرشاد للتثبيت كـ PWA وتنظيف سجل التصفح.
- **زر مسح فوري** يمحو كل شيء محلياً بلا تأكيد مطوّل.
- بلا تحليلات، بلا تتبّع أخطاء يرسل بيانات، بلا خطوط أو موارد من CDN خارجي (تسريب بصمة).

---

## 8. اختبارات القبول

نفّذ هذه كاختبارات آلية تفشل عند الخرق:

1. لا بطاقة `intensity > 0` تُعرض لأي طرف بينما `KeyState === 'locked'`.
2. شجرة عرض واجهة الزوج **متطابقة بايت-ببايت** في الحالتين `locked` و`open` عند تثبيت باقي المدخلات.
3. لا حقل باسم يطابق `/streak|progress|completed_days|score|level/` في أي نموذج مخزَّن.
4. تجاوز بطاقة لا يُنتج أي عنصر واجهة جديد ولا أي نص.
5. ارتفاع السقف مستحيل بلا فعل صريح من الزوجة؛ لا مسار زمني يرفعه.
6. لا طلب شبكة إلى نطاق غير نطاق التطبيق.
7. كل بطاقة `kind: 'text'` لها `source.reviewed === true`؛ وإلا تفشل عملية البناء.
8. نص أي إشعار لا يحتوي محتوى بطاقة ولا اسم قسم.
9. جهاز الزوج لا يحتوي حقل `KeyState` في أي مخزَن.
10. بعد ٢١ يوماً بلا تغيّر في المقاييس، يدخل التطبيق وضع السكون تلقائياً.

---

## 9. ترتيب التنفيذ

1. مخطط `Card` + محمّل قاعدة المعرفة + تحقق البناء (اختبار ٧).
2. نموذج الحالة وإشارات القراءة الخفيفة.
3. سقف الشدة وحلقة الاختيار (اختبارات ١، ٢، ٥).
4. شاشة الميثاق.
5. مسارا الزوجة والزوج ببطاقة يومية واحدة.
6. حلقة التعلّم + وضع السكون.
7. إعادة تأهيل المساحة المشتركة بعد حذف المكافأة.
8. التمويه والخصوصية وزر المسح.

**ابدأ بالخطوات ١–٣ واعرضها قبل المتابعة.** وابنِ قاعدة المعرفة بـ ١٥ بطاقة أولية موزّعة على الحالات، مع ترك مصادر النصوص التراثية فارغة موسومة `TODO_REVIEW` — لتُملأ بعد مراجعة مختص، ولا تُشحن قبلها.
