/**
 * Sakan (سَكَن) — Unified Camouflage Constants (التمويه الموحَّد)
 *
 * SPEC §8.1: "توحيد التمويه بالكامل"
 *
 * كل عنوان صفحة، واسم PWA، ونص إشعار يجب أن يستخدم SAKAN_CAMOUFLAGE_NAME
 * حرفياً — لا مرادفات، لا وصف علاجي، لا كلمة "سكن" أو مشتقاتها.
 *
 * الاستخدام:
 *   src/app/sakan/layout.tsx       — عنوان التبويب وسجل المتصفح
 *   src/lib/sakan/notifications.ts — نص الإشعارات
 *   src/app/layout.tsx             — توجيه font-src في CSP
 *
 * AT6 يتحقق من:
 *   ١. SAKAN_CAMOUFLAGE_NAME لا يحتوي على كلمة كاشفة واحدة.
 *   ٢. CSP_FONT_SRC لا يتضمن أي نطاق https:// خارجي.
 */

// ─── الاسم المحايد الموحَّد ───────────────────────────────────────────────────

/**
 * الاسم الظاهر في التبويبات، وسجل المتصفح، وشاشة الـ PWA، وأي إشعار.
 * يجب أن يكون هو نفسه دون استثناء في كل مكان.
 * AT6 يضمن أن هذا الاسم لا يحتوي على كلمة كاشفة.
 */
export const SAKAN_CAMOUFLAGE_NAME = "محاكي تونس";

// ─── CSP font-src ─────────────────────────────────────────────────────────────

/**
 * توجيه font-src الموحَّد في سياسة CSP.
 * next/font/google يُضمِّن الخط في وقت البناء — لا طلب شبكي إلى Google وقت التشغيل.
 * AT6 يضمن أن هذا الثابت لا يحتوي على https:// بأي صورة.
 */
export const CSP_FONT_SRC = "'self' data:";

// ─── قائمة الكلمات الكاشفة ───────────────────────────────────────────────────

/**
 * كلمات يُفصح وجودها عن طبيعة التطبيق العلاجية.
 * AT6 و AT8 يتحققان من غياب هذه الكلمات في العناوين والإشعارات.
 *
 * القائمة متعمَّد إبقاؤها محدودة بالكلمات الكاشفة الفعلية،
 * لا بكل كلمة مرتبطة بالموضوع.
 */
export const REVEALING_WORDS: readonly string[] = [
  "سكن",             // جذر الاسم بدون تشكيل
  "مساحة آمنة",
  "علاجي",
  "علاقة زوجية",
  "ألفة",
  "اطمئنان",
] as const;
